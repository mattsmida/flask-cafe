{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true
  },
  // The edge function is Deno code with npm: specifiers — not part of the app.
  "exclude": ["supabase/functions"]
}
